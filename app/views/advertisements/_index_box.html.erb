<% @advertisements.each do |advertisement| %>
	<div class="advertisement col-md-3">
   	  <%= advertisement.web_link.present? ? link_to(image_tag(advertisement.image.url(:thumbnail), :class=>"img-responsive index_box_image"), advertisement.web_link) : image_tag(advertisement.image.url(:thumbnail), :class=>"img-responsive index_box_image") %>	
  	</div>
<% end %>