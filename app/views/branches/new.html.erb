
<h2>Add Branch for <%= @store.name %></h2>
<%= form_for(@store, url: (@store.new_record? ? stores_path : store_path(I18n.locale, @store)) ) do |f| %>
	<%= render 'form', :f => f %>

	<%= f.submit %>
<% end %>