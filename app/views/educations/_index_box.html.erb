<% @educations.each_with_index do |education, index| %>
	<div class="box col2">
		<%= link_to(image_tag(education.image.url(:thumbnail), :class=>"img-responsive index_box_image"), education.web_link)%>	
	</div>
<% end %>